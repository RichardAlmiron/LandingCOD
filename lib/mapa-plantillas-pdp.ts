/**
 * MAPA CENTRALIZADO DE PLANTILLAS PDP
 * 
 * ⚠️  ARCHIVO DE COMPATIBILIDAD — Re-exporta desde el registry único.
 * 
 * Este archivo se mantiene para no romper imports existentes.
 * Todo ahora está en: lib/plantilla-registry.ts
 */

// Re-exports desde registry único
export { resolverComponentePDP, extraerVariante, getCodigosRegistrados, existePlantilla, obtenerNicho } from '@/lib/plantilla-registry';
